name: Android release

# Tag builds are deliberately NOT path-filtered: a version-bump-only release commit still needs an APK.
on:
  push:
    tags: ["v*"]

permissions:
  contents: write

concurrency:
  group: android-release-${{ github.ref }}
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: android
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: "17"
      - uses: android-actions/setup-android@v3
        with:
          packages: "platforms;android-37.0 build-tools;36.0.0 platform-tools"
      - uses: gradle/actions/setup-gradle@v4
        with:
          cache-read-only: ${{ github.ref != 'refs/heads/main' && !startsWith(github.ref, 'refs/tags/') }}

      - name: Unit tests
        run: ./gradlew :app:testDebugUnitTest --no-daemon

      - name: Decode release keystore
        env:
          KEYSTORE_B64: ${{ secrets.RELEASE_KEYSTORE_BASE64 }}
        run: |
          test -n "$KEYSTORE_B64"
          printf '%s' "$KEYSTORE_B64" | base64 --decode > "$RUNNER_TEMP/release.jks"
          chmod 600 "$RUNNER_TEMP/release.jks"

      - name: Build signed release APK
        env:
          LUXRAMP_VERSION: ${{ github.ref_name }}
          RELEASE_STORE_FILE: ${{ runner.temp }}/release.jks
          RELEASE_STORE_PASSWORD: ${{ secrets.RELEASE_KEYSTORE_PASSWORD }}
          RELEASE_KEY_ALIAS: ${{ secrets.RELEASE_KEY_ALIAS }}
          RELEASE_KEY_PASSWORD: ${{ secrets.RELEASE_KEY_PASSWORD }}
        run: |
          ./gradlew :app:assembleRelease --no-daemon
          cp app/build/outputs/apk/release/app-release.apk "../LuxRamp-${GITHUB_REF_NAME}.apk"
          cp app/build/outputs/mapping/release/mapping.txt "../LuxRamp-${GITHUB_REF_NAME}-mapping.txt"
          sha256sum "../LuxRamp-${GITHUB_REF_NAME}.apk" > "../LuxRamp-${GITHUB_REF_NAME}.apk.sha256"

      - name: Attach APK to GitHub release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            LuxRamp-${{ github.ref_name }}.apk
            LuxRamp-${{ github.ref_name }}.apk.sha256
            LuxRamp-${{ github.ref_name }}-mapping.txt
